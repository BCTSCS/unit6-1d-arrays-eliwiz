package Data;
public class StopBox { 
private Album[] album;
private DataAnalyzer dataAnalyzer; // Injecting the class 
 // Constructor: Loads birds from files
 public StopBox(String albumNames, String artistNames, String genreNames) { 
       // Initialize birds array and populate it with Bird objects
            album = new Album[names.size()];
            for (int i = 0; i < album.length; i++) {
                album[i] = new Album(names[i], colors[i], diets[i], st[i]);
            }

}


// FUNCTIONS

}
